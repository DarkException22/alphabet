{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build/**", "*.d.ts"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "test": {
      "dependsOn": ["build"],
      "outputs": []
    },
    "lint": {
      "outputs": []
    },
    "clean": {
      "cache": false
    },
    "docs": {
      "outputs": ["docs/**"]
    },
    "examples": {
      "dependsOn": ["build"],
      "outputs": ["examples/**"]
    },
    "publish": {
      "dependsOn": ["build", "test"],
      "outputs": []
    }
  }
}